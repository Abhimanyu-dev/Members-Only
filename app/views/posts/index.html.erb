<section class="section">
  <div class="container">
    <div class="columns">
      <% if user_signed_in? %>
        <%= render "profile" %>
        
        <% end %>
        
      <%= render "feed", posts: @posts, post: @post %>
    </div>
  </div>
</section>